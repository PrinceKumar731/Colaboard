server.port=8080
spring.application.name=colaboard-backend

# Redis
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.password=${REDIS_PASSWORD:}
spring.data.redis.ssl.enabled=${REDIS_SSL:false}

# JWT
app.jwt.secret=${JWT_SECRET:change-this-super-secret-key-change-this-super-secret-key}
app.jwt.expiration-minutes=${JWT_EXPIRATION_MINUTES:120}

# Google OAuth
app.google.client-id=${GOOGLE_CLIENT_ID:}

# PostgreSQL
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Frontend
app.frontend.url=${FRONTEND_URL:http://localhost:5173}

# Periodic room state rebroadcast. Live drawing is incremental; this is a slower
# safety sync for existing clients without rebroadcasting full history on every draw.
app.room.state-sync-interval-ms=${ROOM_STATE_SYNC_INTERVAL_MS:5000}
